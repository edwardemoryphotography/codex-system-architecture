# Codex Reality Remediation Completion Plan

**Goal:** Close every unresolved review defect from PR #19, make provenance machine-enforced, repair Supabase, verify the deployed user flows, and identify unsupported personal claims across Eddie's public repositories and deployments.

**Constraints:** Preserve the 59 canonical paths and live UUIDs. Never introduce mock, sample, placeholder, or invented personal data. Previously applied migrations remain immutable; all database changes are forward-only.

## Task 1: Lock the five review findings with regression tests

**Files:** `src/lib/supabase.test.ts`, `scripts/generate-codex-content-migration.test.ts`

1. Add tests proving canonical parent IDs resolve to live UUIDs.
2. Add tests proving embedded recent-document and bookmark rows receive canonical titles, bodies, and provenance.
3. Add tests proving unmatched canonical rows are read-only in live mode and never send `corpus-*` IDs to Supabase writes.
4. Add generator tests covering CRLF H1 parsing and content containing the old `$codex$` delimiter.
5. Run the focused tests and confirm they fail for the intended defects before implementation.

## Task 2: Implement the five fixes and provenance contract

**Files:** `src/types/index.ts`, `src/content/codexDocumentBodies.ts`, `src/content/codexCorpus.ts`, `src/content/codexReality.test.ts`, `src/lib/supabase.ts`, `scripts/generate-codex-content-migration.mjs`

1. Add required machine-readable provenance fields using only `verified`, `repository_evidence`, `concept`, and `unknown`.
2. Parse and validate the existing real evidence metadata for all 59 canonical documents.
3. Make canonical copy and provenance override stale live and embedded Supabase document rows.
4. Resolve parent IDs through live UUIDs and mark unmatched canonical rows read-only.
5. Reject persistence calls for read-only or `corpus-*` document IDs.
6. Replace static dollar quoting with SQL-safe single-quote escaping and make H1 parsing CRLF-safe.
7. Run focused tests, the full test suite, typecheck, lint, and production build.

## Task 3: Generate, apply, and verify the forward-only Supabase repair

**Files:** `supabase/migrations/20260715*_enforce_codex_provenance.sql`, `README.md`, `CLAUDE.md`

1. Check current Supabase documentation and project access.
2. Generate a new forward migration from the canonical corpus; do not edit the existing July 14 migration.
3. Add/backfill constrained provenance columns and update all 59 canonical rows atomically.
4. Apply the migration through the connected Supabase project.
5. Query row count, allowed provenance values, non-empty evidence basis, review dates, corrected gear, and absence of known false claims.
6. Run database advisors and record any unrelated existing findings separately.

## Task 4: Publish and verify production end to end

1. Commit and push the remediation branch; open a focused PR and merge after checks pass.
2. Confirm the Vercel production deployment uses the merged commit.
3. Exercise document navigation, search, bookmarks, recent pages, notes, and command palette against production.
4. Confirm stale titles and false bodies cannot surface through any tested route.
5. Inspect runtime/build logs for errors generated by the tested flows.

## Task 5: Audit all public repositories and deployed sites

1. Inventory every public repository owned by `edwardemoryphotography`.
2. Search active source and public-facing content for known fabricated claims, simulated personal metrics, fake equipment, unsupported project status, and unlabeled personal assertions.
3. Inventory connected Vercel production projects and inspect reachable public deployments.
4. Classify every hit as active, historical evidence, test fixture, verified, or needs remediation.
5. Save a dated audit report with repository/site coverage, exact evidence, corrections completed, and unresolved access blockers.

## Completion gate

Do not report completion until code checks pass freshly, the Supabase queries prove the repair, production flows have direct evidence, and every inventoried public repository/site has an audit disposition.
